[defaults]
inventory = ./inventories/hosts
host_key_checking = False
retry_files_enabled = False
roles_path = ./roles
collections_paths = ./collections

[privilege_escalation]
become = False              #we may change this back later
become_method = sudo
become_ask_pass = False
